providers = ["python"]

[phases.setup]
nixPkgs = ["pkgs.python311"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  "/opt/venv/bin/python -m pip install --upgrade pip",
  "/opt/venv/bin/python -m pip install -r requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python main.py"
